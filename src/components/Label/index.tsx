// types
import { LabelProps } from "../../types/Label";

// Styles
import { Container } from "./styles";

export default function LabelForm({ children }: LabelProps) {
  return <Container>{children}</Container>;
}
